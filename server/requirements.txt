# Core Web & AI
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
numpy>=1.24.0
scipy>=1.10.0
google-genai>=0.3.0
opencv-python-headless>=4.8.0

# Pose Estimation (YOLO26)
ultralytics>=8.3.0

# --- INSTALL MANUALLY (hardware-dependent, not pip-installable from this file) ---
# torch and torchvision require CUDA-specific wheels.
# For RTX 5080 / Blackwell (sm_120), use cu128 wheels:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
