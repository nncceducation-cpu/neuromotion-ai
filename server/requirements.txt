
# =============================================================================
# NEUROMOTION AI - BACKEND DEPENDENCIES
# =============================================================================
#
# INSTALLATION (PowerShell):
#
# 1. Basic deps:
#    pip install -r requirements.txt
#
# 2. PyTorch (RTX 5080 / Blackwell needs CUDA 12.8+ wheels):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#    Verify: python -c "import torch; print(torch.cuda.is_available())"
#
# =============================================================================

# Core Web & AI
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
numpy>=1.24.0
scipy>=1.10.0
google-genai>=0.3.0
opencv-python-headless>=4.8.0

# YOLO26x-Pose (replaces OpenMMLab ViTPose stack)
ultralytics>=8.3.0

# HARDWARE DEPENDENT LIBRARIES
# Install manually using Step 2 above — not listed here to prevent
# pip from pulling incompatible CPU-only versions.
#
# torch
# torchvision
